import { StyleSheet } from "react-native"

export const stylesRating = StyleSheet.create({
    container: {
        width: "100%",
        borderRadius: 10,
        justifyContent: "center",
        alignItems: "center",
        flexDirection: "row",
        padding: 8,
        textAlign: "center",
        fontSize: 16,
        color: "#fff",
        fontWeight: "bold",
        marginBottom: 25,
        marginTop: 25,
    },
    ratingText: {
        fontSize: 12,
        fontWeight: "bold",
        marginRight: 8,
        color: "#fff",
    },
})
