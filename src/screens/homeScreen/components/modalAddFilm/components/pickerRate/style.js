import { StyleSheet } from "react-native"

export const stylesPickerRate = StyleSheet.create({
    pickerContainer: {
        backgroundColor: "#091057",
        borderRadius: 50,
        borderWidth: 2,
        borderColor: "#EC8305",
        paddingHorizontal: 10,
        overflow: "hidden",
        opacity: 0.8,
    },
    picker: {
        color: "#fff",
    },
})
