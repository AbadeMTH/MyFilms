import { StyleSheet } from "react-native"

export const stylesTitle = StyleSheet.create({
    container: {
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: 'red'
    },
    filmsText: {
        fontSize: 20,
        fontWeight: "bold",
        textAlign: 'center'
    },
    line: {
        backgroundColor: "#000",
        height: 10,
        marginHorizontal: 20,
        marginTop: 10,  
        width: 100
    },
})
